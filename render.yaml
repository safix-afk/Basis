services:
  - type: web
    name: basis-backend
    env: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: chmod +x start.sh && ./start.sh
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 10000

